# ⚠️  DO NOT COMMIT THIS FILE — copy to env.secret and fill in values
# These values are injected by AWS Secrets Manager in CI

# Fastlane Match (encrypts certs/profiles at rest in S3)
MATCH_PASSWORD=your-match-password

# Apple ID web session — only needed by the `produce` lane (create_app).
# Apple's API key cannot create apps; produce uses a web session instead.
# Generate FASTLANE_SESSION with: bundle exec fastlane spaceauth -u <apple-id>
# (prompts for a 2FA code, prints a YAML cookie string; ~30 day lifetime).
FASTLANE_USER=your-apple-id
FASTLANE_PASSWORD=your-apple-id-password
FASTLANE_SESSION=your-spaceauth-session-yaml

# Firebase
FIREBASE_CLI_TOKEN=your-firebase-cli-token

# App Store Connect API Key
APP_STORE_CONNECT_API_KEY_KEY_ID=your-key-id
APP_STORE_CONNECT_API_KEY_ISSUER_ID=your-issuer-id
# Must be double-quoted — Dotenv strips the backslash from an unquoted \n,
# corrupting the PEM. Quoting preserves the literal \n that fastlane's
# app_store_connect_api_key action expands back into real newlines.
APP_STORE_CONNECT_API_KEY_KEY="-----BEGIN EC PRIVATE KEY-----\nyour-key-content\n-----END EC PRIVATE KEY-----"
APP_STORE_CONNECT_API_KEY_IS_KEY_CONTENT_BASE64=false

# Android Keystore
KEYSTORE_PATH=keystore/release.keystore
KEYSTORE_PASSWORD=your-keystore-password
KEY_ALIAS=your-key-alias
KEY_PASSWORD=your-key-password
